/*import { User, StreamVideoClient, StreamVideo, StreamCall, CallContent } from '@stream-io/video-react-native-sdk';
import { Text } from 'react-native';

const apiKey = "hpbruqux38vs";
const userId = "JgMijJ6CEmRLPSe6apZKiPO6aMy2";
const token = "authentication-token";
const callId = "default_628d8722-4e4e-4ec8-895a-41047cec772f";
const user: User = { id: userId };

const client = new StreamVideoClient({ apiKey, user, token });
const call = client.call("default", callId);
call.join({ create: true });

export default function CallScreen()
{
    return (
      <StreamVideo client={client}>
      <StreamCall call={call}>
        <CallContent />

      </StreamCall>
    </StreamVideo>

    );
}*/